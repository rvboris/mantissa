{
  "name": "mantissa",
  "devDependencies": {
		"lerna": "^3.4.3"
  },
  "scripts": {
    "bs": "lerna bootstrap --hoist",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint"
  }
}
