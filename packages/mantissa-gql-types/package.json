{
  "name": "@mantissa/gql-types",
  "version": "1.0.0",
  "description": "@mantissa/gql-types",
  "license": "MIT",
  "repository": "",
  "author": {
    "name": "Boris Ryabov",
    "email": "rv.boris@gmail.com"
  },
  "keywords": [
    "types",
    "graphql"
  ],
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "clean": "rimraf lib && rimraf coverage",
    "format": "prettier --write \"{src,__tests__}/**/*.ts\" --single-quote --trailing-comma es5",
    "lint": "tslint --force --format verbose \"src/**/*.ts\"",
    "prepublishOnly": "npm run build",
    "prebuild": "npm run clean && npm run generate && npm run format && npm run lint && echo Using TypeScript && tsc --version",
    "build": "tsc",
    "generate": "ts-node ./src/generate.ts",
    "test": "jest",
    "coverage": "jest --coverage",
    "watch": "npm run build -- --watch",
    "watch:test": "jest --watch"
  },
  "dependencies": {
    "@gql2ts/from-schema": "^1.10.1",
    "@gql2ts/language-typescript": "^1.9.0",
    "@types/glob": "^7.1.1",
    "glob": "^7.1.3",
    "merge-graphql-schemas": "^1.5.8",
    "typescript": "^3.2.4"
  },
  "devDependencies": {
    "@types/jest": "^23.3.13",
    "@types/node": "^10.12.18",
    "jest": "^23.6.0",
    "prettier": "^1.16.1",
    "rimraf": "^2.6.3",
    "ts-jest": "^23.10.5",
    "tslint": "^5.12.1",
    "tslint-config-prettier": "^1.17.0"
  },
  "engines": {
    "node": ">=10.0.0"
  },
  "jest": {
    "verbose": true,
    "transform": {
      ".(ts)": "ts-jest"
    },
    "testRegex": "(/__tests__/.*|\\.(test|spec))\\.(ts|js)$",
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "coverageDirectory": "../coverage",
    "testEnvironment": "node"
  }
}
