{
  "name": "mantissa",
  "devDependencies": {
    "lerna": "^3.13.3"
  },
  "scripts": {
    "bs": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint"
  }
}
